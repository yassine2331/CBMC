# Experiment 02 — Deeper encoder

## Hypothesis
Adding more residual layers per block (`layers_per_block=3`) and doubling
the latent channels (`latent_channels=8`) should give the model more
capacity and produce lower reconstruction error.

## Config highlights
- `layers_per_block = 3` (baseline: 2)
- `latent_channels = 8` (baseline: 4)

## Results
_Fill in after running the experiment._

| Metric | Value |
|--------|-------|
| Reconstruction loss | — |
| KL divergence | — |

## Observations
_Write what you noticed here._

## Next steps
_What would you try next based on these results?_
